#!/usr/bin/make -f
%:
	dh $@ --with=systemd

override_dh_strip:
	# noop

override_dh_systemd_enable:
	dh_systemd_enable -pcoreroller-rollerd --name=rollerd rollerd.service

override_dh_systemd_start:
	dh_systemd_start -pcoreroller-rollerd rollerd.service
